# from django.urls import path
# from .views import ArtistRegisterView, ArtistLoginView

# urlpatterns = [
#     path('register/', ArtistRegisterView.as_view(), name='register'),
#     path('login/', ArtistLoginView.as_view(), name='login'),
# ]
